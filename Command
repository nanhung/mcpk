##
git rest --hard HEAD # the last commit

git reset --hard f414f31
git reset --soft HEAD@{1}
git commit -m "Reverting to the state of the project at f414f31"


## MCsim
tar xf mcsim-5.6.5.tar #unzip

#install
./configure
make
make check

##Install R
Downloaded with
wget http://cran.r-project.org/src/base/R-3/R-3.0.3.tar.gz
tar -xzf R-3.0.3.tar.gz
cd R-3.0.3

Installed withcd 
sudo apt-get update
sudo apt-get install r-base

Launched as
someone:~/workspace/R-3.0.3 $ R

## Git
git checkout <brance>
git branch -m 

git remote add origin https://github.com/user/repo.git
# Set a new remote

git remote -v
# Verify new remote

git push origin master

git commit TCE.2.1.pop.model.c
git status
git push origin master

git add .
git commit
git push